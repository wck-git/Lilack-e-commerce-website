To manually add an admin user to the system, go to

layouts -> signUpInc.php -> line 134 

and replace the userRoleID of 3 to 1 if there is no admin in the database. Change it back to 1 when there is one admin.

